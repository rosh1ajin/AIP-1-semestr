#include <iostream>

using namespace std;

struct complex {
    int mat,imag;
};

void addition(complex a, complex b) {
    float sumMat, sumImag;
    sumMat = a.mat + b.mat;
    sumImag = a.imag + b.imag;
}

int main() {
    return 0;
}
